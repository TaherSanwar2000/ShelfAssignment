import { View, Text } from 'react-native'
import React from 'react'

const Chart = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{fontSize: 36, color: '#BCBCBC'}}>Chart</Text>
    </View>
  )
}

export default Chart