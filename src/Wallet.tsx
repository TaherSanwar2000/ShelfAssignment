import { View, Text } from 'react-native'
import React from 'react'

const Wallet = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
    <Text style={{fontSize: 36, color: '#BCBCBC'}}>Wallet</Text>
  </View>
  )
}

export default Wallet